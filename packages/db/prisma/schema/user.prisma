model User {
  id       String  @id @default(uuid())
  name     String
  email    String  @unique
  password String?

  insertedAt DateTime @default(now())
  updatedAt  DateTime @updatedAt

  @@map("users")
}
